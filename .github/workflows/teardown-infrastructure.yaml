name: Teardown Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment (dev | prod)'
        required: true
        type: choice
        options:
          - dev
          - prod
      component:
        description: 'Infrastructure component to teardown'
        required: true
        type: choice
        options:
          - pgadmin
          - ollama
          - dashboard

jobs:
  teardown:
    name: Teardown ${{ inputs.component }} → ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      contents: read

    steps:
      - name: Checkout infra charts
        uses: actions/checkout@v6
        with:
          repository: pavelsamodurov/resonancelab-infra
          ref: main

      - name: Set up Helm
        if: inputs.component == 'dashboard'
        uses: azure/setup-helm@v5.0.0

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Teardown pgadmin
        if: inputs.component == 'pgadmin'
        run: |
          kubectl delete -f infrastructure/pgadmin/ \
            --namespace=${{ inputs.environment }} \
            --ignore-not-found
          kubectl delete secret pgadmin-secret \
            --namespace=${{ inputs.environment }} \
            --ignore-not-found

      - name: Teardown ollama
        if: inputs.component == 'ollama'
        run: |
          kubectl delete -f infrastructure/ollama/ \
            --namespace=${{ inputs.environment }} \
            --ignore-not-found
          kubectl delete configmap ollama-config \
            --namespace=${{ inputs.environment }} \
            --ignore-not-found

      - name: Teardown dashboard (Headlamp)
        if: inputs.component == 'dashboard'
        run: |
          helm uninstall headlamp \
            --namespace kube-system \
            --ignore-not-found
          kubectl delete -f infrastructure/dashboard/serviceaccount.yaml \
            --ignore-not-found